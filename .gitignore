# Dependencies
node_modules/

# Build output
dist/
.vite/

# Environment
.env
.env.local
.env.*.local

# Raw assets — large itch.io packs, never committed (re-fetch via scripts/fetch-itch.mjs
# or scripts/import-from-voxel-realms.mjs). The raw-assets/ directory itself stays
# tracked via raw-assets/.gitkeep so the pipeline scripts find it.
raw-assets/archives/
raw-assets/extracted/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Capacitor
android/
ios/

# Coverage
coverage/

# Vite generated types
vite.config.d.ts

# Playwright
.playwright-mcp/
playwright-report/
test-results/

# Screenshots from visual testing
*.png
!public/*.png

# Expo (migration canceled — leftover local-only artifacts)
.expo/
expo-env.d.ts

# Session-local memory/logs (not a shared artifact)
.remember/
memory-bank/
.ralph-tui/

# Claude Code: share hooks/skills/settings/directives; ignore transient
# runtime state + per-session markers inside .claude/.
.claude/state/
.claude/logs/
.claude/cache/
.claude/tmp/
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json
.claude/.last-*

# Ad-hoc scratch files accidentally committed to public/
public/test.txt
public/testdir/
